<Window x:Class="DockPanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DockPanel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!--LastChildFill="True"使用最後一個元素填滿剩餘空間-->
        <!--DockPanel.Dock="Bottom" 指定在哪個位子只有這一個Panel才能用-->
        <DockPanel LastChildFill="True">
            <Button DockPanel.Dock="Top" Content="Top Button"/>
            <Button DockPanel.Dock="Top" Content="A centered TOP Button" HorizontalAlignment="Center"/>
            <Button DockPanel.Dock="Top" Content="A centered LEFT TOP Button" HorizontalAlignment="Left"/>
            <Button DockPanel.Dock="Bottom" Content="Bottom Button"/>
            <Button  DockPanel.Dock="Left" Content="Left Button"/>
            <Button DockPanel.Dock="Right" Content="Right Button"/>
            <Button  Content="SPEAC"/>
        </DockPanel>
    </Grid>
</Window>
